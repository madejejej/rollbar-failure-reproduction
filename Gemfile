source 'https://rubygems.org'

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'sidekiq'
gem 'rollbar', 
  github: 'madejejej/rollbar-gem',
  ref: '325cb61' # this just raises Net::ReadTimeout
  # ref: 'f72d4b7' # this fixes failsafe

